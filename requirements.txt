claude-agent-sdk
pydantic
fastapi
uvicorn
pyyaml
pytest

# Persistence: PostgreSQL is the system of record, SQLite backs the offline
# tests (app/pipeline/store.py). The binary wheel bundles libpq, which is what
# makes this a one-line install on Windows. Caliber pins the same line.
psycopg[binary]>=3.2

# Step [0] document extraction (deterministic, no model)
pdfplumber>=0.11.10
python-docx>=1.1

# Dev only - regenerating data/fixtures/extraction via scripts/make_extraction_fixtures.py
# reportlab>=4.0
